import {TroopService} from './troop.service';

describe('TroopService', () => {
  // beforeEach(() => TestBed.configureTestingModule({}));
  //
  // it('should be created', () => {
  //   const service: TroopService = TestBed.get(TroopService);
  //   expect(service).toBeTruthy();
  // });
});
