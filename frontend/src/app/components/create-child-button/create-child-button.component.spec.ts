import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateChildButtonComponent } from './create-child-button.component';

describe('CreateChildButtonComponent', () => {
  // let component: CreateChildButtonComponent;
  // let fixture: ComponentFixture<CreateChildButtonComponent>;
  //
  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ CreateChildButtonComponent ]
  //   })
  //   .compileComponents();
  // }));
  //
  // beforeEach(() => {
  //   fixture = TestBed.createComponent(CreateChildButtonComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
  //
  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
