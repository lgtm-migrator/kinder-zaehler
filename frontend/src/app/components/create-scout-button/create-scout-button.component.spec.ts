import {CreateScoutButtonComponent} from './create-scout-button.component';

describe('CreateScoutButtonComponent', () => {
  // let component: CreateScoutButtonComponent;
  // let fixture: ComponentFixture<CreateScoutButtonComponent>;
  //
  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ CreateScoutButtonComponent ]
  //   })
  //   .compileComponents();
  // }));
  //
  // beforeEach(() => {
  //   fixture = TestBed.createComponent(CreateScoutButtonComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });
  //
  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
